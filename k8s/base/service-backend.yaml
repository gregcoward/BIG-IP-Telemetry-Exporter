apiVersion: v1
kind: Service
metadata:
  name: bigip-metrics-backend
  labels:
    app.kubernetes.io/name: bigip-metrics-backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bigip-metrics-backend
  ports:
    - name: http
      port: 8000
      targetPort: http
